/*

*/
#include <iostream>
using namespace std;
#include <math.h>
int main()
{
    char a; // # is a char value.
    a=35;   //35 is the ascii value of '#' and the interger value is assaigned to a and the compiler reads the number value as charcter value. read as ascii value
    for(int i=0;i<4;i++){ //code starts from zero and stop at three(less than 4) so the loop runs four times
        
        for(int i=0;i<4;i++){
        cout<<a<<" ";
    } 
        cout<<endl;
    }


    return 0;
}

